import { CheckCircle } from "phosphor-react"

const SuccessAdmin = ({text}) => (
        <div className='mt-3 text-center mc-color'>
            <CheckCircle size={20} /> {text}
        </div>
)

export default SuccessAdmin